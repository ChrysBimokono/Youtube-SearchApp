# We will Re-create the Youtube Homepage With real Data

Connect to Youtube "Search" API
Create an Ajax request to fetch data
render JSON data on the page with dynamic HTML
Allow user to search(event listener)